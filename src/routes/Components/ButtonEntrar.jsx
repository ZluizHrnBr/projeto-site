

const ButtonEntrar = ({onClick}) => {
    return (
        <div>
            <button className="btn btn-success"  type="submit" onClick={onClick}>Entrar</button>
        </div>
    );
}

export default ButtonEntrar;
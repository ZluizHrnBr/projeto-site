

const ButtonCadastrar = ({onClick}) => {
    return (
        <div>
            <button className="btn btn-success" type="submit" onClick={onClick}>Cadastrar</button>
        </div>
    );
}

export default ButtonCadastrar;